import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Multisports Académie Gestion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
